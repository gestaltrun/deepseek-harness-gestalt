/**
 * Member-question plugin, node half.
 *
 * Deliberately empty. The member-question receiver experience is a browser
 * presentation over the shared question carrier; routing a routed member ask
 * onto a session's question wait is the relay milestone's seam, and this
 * package owns no host capability.
 */

/** Host plugin body — no host-half behavior is owned here. */
export function apply(): void {}
