## Parent

Part of #{{PARENT}}.

## Starting point

Start from the remote upstream-sync baseline after #{{T2}}, #{{T3}}, and the provisional Session binding prerequisite merge.

## Outcome

Move every live Gestalt capability temporarily preserved in `packages/host/apiproxy` to the upstream domain Remote/controller architecture, then delete ApiProxy and its duplicated Session export implementation.

## What to build

- Implement a Member Question Remote in the owning interaction package, assemble it through `packages/api/remotes`, and forward its live events through the explicit Remote event list. Preserve materialization, terminal retry/recovery, workspace binding, settlement, and human-turn admission.
- Port goal fork-seed clearing to the Session controller fork operation.
- Port bounded no-shell Git remote/clone execution to the Workspace controller through the upstream native-command/subprocess interfaces.
- Port tool eligibility/catalog access, companion attachment admission and operation-id idempotency, web-search settings probe, child model selection/queue service behavior, official-provider catalog filtering, and `maxImageDimension` metadata to their owning controllers.
- Use the Gateway's ready/host facts where upstream already owns equivalent behavior. Do not preserve duplicate Host-describe or generic settings code.
- Make session-log-export the single export owner and delete the ApiProxy copy.
- Replace all consumers, bundle rows, docs-owned references, and package dependencies on `@deepseek-ai/dsh-host-apiproxy`.
- Delete `packages/host/apiproxy` only after focused controller/Remote tests prove every retained behavior.

## File ownership

Own `packages/host/apiproxy/**`, `packages/api/session-controller/**`, `packages/api/workspace-controller/**`, `packages/api/settings-controller/**`, `packages/api/gateway/**`, `packages/api/remotes/**`, the Remote-export files in Member Question packages agreed with #{{T3}}, `packages/session-query/session-log-export/**`, and directly related host-side tests. Do not edit Client rendering, Desktop/Mobile UI, root lock/config files, or core Tools/Subagent code.

## Non-goals

- No compatibility ApiProxy facade.
- No Desktop or Client runtime migration.
- No SQLite Session persistence restoration.

## Acceptance criteria

- [ ] Every F1-F12 behavior listed in the parent research plan is either ported to its owner or explicitly removed because upstream already provides the same behavior.
- [ ] `packages/host/apiproxy` is physically absent and no tracked manifest/source/config imports its package name.
- [ ] Generated Typert Remotes expose the new methods and reject invalid wire values at the process boundary.
- [ ] Member Question restart recovery, attachment idempotency, Git command bounds/cancellation, goal-fork clearing, and controller authorization have focused tests.
- [ ] Session export has one implementation.
- [ ] Agent Note, package README/JSDoc, API subsystem docs, generated Remote/type artifacts, and a keyless real Host composition snapshot land.

## Dependencies

Blocked by: #{{T2}}, #{{T3}}, and the provisional Session binding prerequisite.
