# dsh-subagent-route-preauthorization-static

Static Provider for `dsh-subagent-route-preauthorization`. `allowedModels` is validated, deduplicated, and sorted once for the Provider lifetime. The Provider has no Settings dependency and Cordis disposal removes the service.
