/**
 * Bin-only app package: the published entry discovers an external `cordis.yml`
 * and owns process exit. This module exports no composition plugin; the leaf
 * config loads host-webserver, phone-runtime, and phone-stream.
 *
 * @module @deepseek-ai/dsh-phone-capture-wire-demo
 */

export {}
