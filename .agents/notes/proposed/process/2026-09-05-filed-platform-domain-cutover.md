# Agent Note: Filed-domain Platform cutover to beikejiedeliulangmao.top

Status: proposed

English | [中文](2026-09-05-filed-platform-domain-cutover.zh.md)

## Problem

The operated Platform serves `https://www.gestaltrun.com/` from `apps/platform/public` through one ALB HTTPS listener while the gestaltrun.com ICP filing is pending. A filed domain, `beikejiedeliulangmao.top`, is now approved under 辽ICP备19017854号-1 for the authenticated Aliyun account that operates the deployment. Until the origin moves onto the filed domain, production service in mainland China carries filing risk on the current name.

Moving the origin is not a single DNS edit. `PLATFORM_ORIGIN` in Environment `production` is the input the Desktop config writer, the Mobile build, and the Platform listen process all derive their identity from: the Desktop config writer derives the Relay WSS URL as `wss://<origin>/v1/remote-access/relay`, the Mobile bundle bakes `VITE_PLATFORM_ORIGIN`, `VITE_PLATFORM_CALLBACK_URL`, and `VITE_REMOTE_RELAY_WSS_URL` at build time, the Platform listen process derives CORS allowed origins and the `/pair` link origin from the same value, and `production-env.ts` enforces that `PLATFORM_ORIGIN` and `PLATFORM_GITHUB_CALLBACK` share one HTTPS origin with callback path `/v1/account/oauth/github/callback`. Installed Desktop clients and published Mobile builds carry the old origin, and the GitHub OAuth App's callback URL is registered outside the repository.

#480 records that the current ALB/TLS boundary rejects Android System WebView 83 with `net_error -101`. The cutover changes the TLS certificate presented at that boundary, so #480's diagnosis must be revalidated against the new-domain certificate rather than carried over as either evidence or blocker. #415 tracks a release-train alignment and stays open until its own release evidence completes.

The user selected two monthly `ecs.e-c1m2.large` instances behind a new ALB Basic and authorized the task-scoped Aliyun operations needed to stage that replacement. The migration must preserve the durable substrate—RDS, Redis, database identity, signing keys, namespace, accounts, and data—and must retain the old resources until the replacement passes verification and its rollback window closes.

Old-client transition has been verified in code rather than assumed. The login POST carries installation identity only (`packages/platform/platform-account-client/src/index.ts` — `beginLogin` sends `InstallationLoginIdentity` + public key); the server generates the GitHub `authorizationUrl` with its own configured `redirect_uri` and fixed callback (`packages/platform/platform-account-core/src/index.ts` — `authorizeUrl` builds `https://github.com/login/oauth/authorize` with `client_id`, the environment's `callbackUrl`, `state`, and the S256 code challenge); the client-side parser validates only an HTTPS scheme on the returned `authorizationUrl` (`packages/platform/platform-account/src/parsers.ts` — `parseLoginAttemptView` requires `httpsUrl(record.authorizationUrl)`). Consequence: old clients can keep POSTing login attempts and polling on the old hostname while the server emits new-domain OAuth callbacks, provided both hostnames reach the same backend with the same identity; the Environment origin change does not shut the old hostname down; and no second OAuth App is required.

## Proposal

Cut the operated Platform over to `https://www.beikejiedeliulangmao.top` with apex (`beikejiedeliulangmao.top`) support, through the staged sequence below. The user has confirmed the canonical www origin and authorized the domain deployment, formal Desktop/Mobile reissue, two-node monthly ECS replacement, new-instance ALB Basic migration, certificate and DNS work, and necessary task-scoped Aliyun cost optimizations without per-step reconfirmation. The recorded approvals are [the bounded ECS purchase](https://github.com/gestaltrun/deepseek-harness-gestalt/issues/608#issuecomment-5553279192), [the ALB Basic cutover](https://github.com/gestaltrun/deepseek-harness-gestalt/issues/608#issuecomment-5553317796), and [the task-scoped operational authority](https://github.com/gestaltrun/deepseek-harness-gestalt/issues/608#issuecomment-5553326557). Two `ecs.e-c1m2.large` (2 vCPU, 4 GiB) monthly instances, each with a 40 GiB ESSD PL0 system disk, have a combined first-month cap of 135.04, no annual commitment, and no default auto-renew. RDS and Redis instances, data, database identity, namespace, signing keys, accounts, and durable state remain unchanged. Old ECS, Standard ALB, EIPs, and other shared resources are released only after the replacement path passes acceptance, dependencies are confirmed, and the rollback window closes. This note and its PR perform no cloud mutation; the separately assigned infrastructure executor owns the authorized staged operations.

Canonical authority after cutover:

- `PLATFORM_ORIGIN` (Environment `production`) becomes `https://www.beikejiedeliulangmao.top`; `PLATFORM_GITHUB_CALLBACK` becomes `https://www.beikejiedeliulangmao.top/v1/account/oauth/github/callback`.
- Environment `desktop-release` updates the same two names, `PLATFORM_ORIGIN` and `PLATFORM_GITHUB_CALLBACK` — the Desktop config projection reads `PLATFORM_*` names, not `VITE_*`. Environment `mobile-release` updates `VITE_PLATFORM_ORIGIN`, `VITE_PLATFORM_CALLBACK_URL`, and `VITE_REMOTE_RELAY_WSS_URL` to the same www origin, keeping `VITE_REMOTE_RELAY_WSS_URL = wss://www.beikejiedeliulangmao.top/v1/remote-access/relay` consistent with the Desktop-derived relay URL.
- The GitHub OAuth App (client id `Ov23lip9LTmnFuFpFeeV`) callback URL is re-registered to the new callback path. This App is distinct from the repository's automation GitHub Apps and is not replaced; the code-verified transition requires no second OAuth App.
- Unconditional preservation: `PLATFORM_POSTGRES_DATABASE` (database identity), `PLATFORM_IDENTITY_NAMESPACE`, `PLATFORM_TOKEN_SIGNING_KEY`, `PLATFORM_POLLING_SIGNING_KEY`, and all durable data. Accounts, installations, pairings, and durable state survive because the identity that keys them is preserved. RDS and Redis stay; only their billing is optimized.
- Selected topology and authorized infrastructure migration: two monthly `ecs.e-c1m2.large` (2 vCPU, 4 GiB) ECS instances, each with a 40 GiB ESSD PL0 system disk, behind a new ALB Basic. Official `DescribePrice` reconciles 47.52 compute plus 20 for the disk to 67.52 each and 135.04 for the pair. The combined first-month ECS amount must not exceed 135.04; a higher checkout stops for a new decision. The instances use no annual term and no default auto-renew. The Basic ALB uses a staged new-instance cutover because Standard cannot downgrade in place; its approved planning basis is approximately 67.56 per 720-hour month—35.28 Basic instance, 28.80 for two EIP holding fees, and estimated 3.48 LCU—with public traffic additional, variable LCU usage, and discounts unconfirmed. RDS and Redis instances and data remain. The old ECS and Standard ALB stay available through replacement acceptance and rollback; their disclosed overlapping charges are part of the safe migration order.
- The three TXT records and any other DNS records on the zone are preserved verbatim; only the two old A records (`120.77.49.2`, apex and www) are replaced, with recorded pre-change values for rollback. Both hostnames migrate; canonical authority stays the www origin. The pre-disabled `www.gestaltrun.com` record is not silently re-enabled.
- The gestaltrun.com certificate and Standard ALB stay intact as the old frontend until old-client transition evidence and the rollback window close; the origin switch does not retire that path, and its identity namespace remains unchanged. The old DNS remains disabled unless separately authorized.
- Full Aliyun operational authority is task-scoped: the infrastructure executor may create and configure the selected ECS pair and Basic ALB, obtain and attach the certificate, update the two authorized DNS records, apply necessary cost optimizations, and perform the staged cutover without per-step asks. The authority excludes unrelated spending, bank or card access, arbitrary deletion, annual commitments, and default auto-renew. An amount above the ECS cap, insufficient balance, inaccessible owner login, or an unachievable safety requirement is a blocker rather than permission to bypass the constraint.
- The brand name, READMEs, historical release notes, and historical documentation URLs that reference gestaltrun.com are not renamed. The served homepage canonical and current discovery metadata move to `https://www.beikejiedeliulangmao.top/` because both apex and www service migrate and www is the canonical Platform authority.

Apex behavior: the apex hostname gets a valid certificate and DNS reachability so that apex HTTPS does not fail, but the canonical authority is the www origin. Whether apex redirects to www or serves the product directly is decided during Stage 1 verification against the new Basic listener; OAuth POST callbacks and Relay WSS are never pointed at the apex.

## Staged cutover

Stage 0 — Freeze and record (no production mutation). Record current apex/www A values (`120.77.49.2`, TTL 600 s), the three TXT records, the ALB listener id and its current certificates, server group, ECS instance ids, and the current GitHub OAuth App callback URL. Re-fetch `origin/master` and confirm the release-train state (PR #584 / plan 0012 selecting Desktop 0.1.16) so this cutover does not interleave with that train.

Stage 0.5 — Readiness and bounded-cost validation, followed by authorized staging. Recheck the time-sensitive account balance and final order terms immediately before mutation. Stage two monthly `ecs.e-c1m2.large` instances with 40 GiB ESSD PL0 system disks only when their combined first-month checkout is at most 135.04, with no annual term and no default auto-renew; validate the 2 vCPU/4 GiB shape against observed utilization while the old nodes remain available. Create the approved new ALB Basic only after confirming its current terms and dependency ownership. Basic has already been verified to support HTTPS, WebSocket, HTTP/2, XFF append, two-backend health checks, and the SYSTEM preset `tls_cipher_policy_1_2_strict_with_1_3`; no TLS weakening is accepted. The planning cost basis is approximately 67.56 per 720-hour month including 35.28 instance cost, 28.80 for two EIP holding fees, and estimated 3.48 LCU, with public traffic additional, variable LCU usage, and discounts unconfirmed. Preserve RDS and Redis; billing changes may proceed only when they preserve their instances, data, and identities within the task-scoped cost-optimization authority. The corrected 2026-09-05T16:04Z report showed balance +86.52 and no ALB financial locks, but the executor uses the current recheck rather than treating that observation as durable.

Stage 1 — New Basic ALB, certificate, and DNS staging (Aliyun, assigned infrastructure executor). Create the approved Basic instance, attach the two replacement ECS backends, reproduce the verified HTTPS, WebSocket, HTTP/2, XFF, health-check, and `tls_cipher_policy_1_2_strict_with_1_3` behavior, and retain the Standard ALB as rollback. Issue one HTTPS certificate covering `beikejiedeliulangmao.top` and `www.beikejiedeliulangmao.top`, attach it to the Basic HTTPS listener, and validate both names before DNS cutover. Replace only the two authorized A records so apex and www resolve through the new frontend; preserve the three TXT records, all unrelated DNS, TTL values, and the old A values. Verify authoritative and public resolution, TLS handshake and hostname validation for both names, backend health, `/readyz`, Relay WebSocket behavior, and the unchanged old path before proceeding. Do not release the Standard ALB, its EIPs, or shared resources during this stage.

Stage 2 — Platform origin switch (GitHub Environment `production` only; no deploy yet). Update `PLATFORM_ORIGIN` and `PLATFORM_GITHUB_CALLBACK` vars; re-register the OAuth App callback. Run the platform-deploy validate job (source CLI `production-env-cli.ts` rejects mismatched origin/callback before any ECS apply). Verify `/readyz` on the new www origin against the still-running old deployment before proceeding. This stage is reversible by restoring the two Environment variables and the OAuth App callback URL. The switch changes what new server responses carry: after the deploy that consumes them, login attempts initiated on either hostname receive a server-generated `authorizationUrl` whose `redirect_uri` is the new-domain callback, while the client parser accepts any HTTPS `authorizationUrl`. The switch does not stop the old hostname from serving, and no second OAuth App is created for the transition. Login attempts already pending at cutover time reference the pre-switch callback; they may need a client restart to re-initiate against the post-switch flow, and Stage 5 covers that restart as part of the transition rather than as a service guarantee.

The old DNS records on gestaltrun.com are currently DISABLED — an unresolved deliberate state owned outside this note. This note does not re-enable them: any change to that state is a separate explicit user decision with its own record. While the old hostnames stay disabled, old clients cannot reach the Platform directly; the Desktop updater is independent of the Platform origin (it updates through GitHub Releases), so an installed old-origin Desktop can still update to the reissued release and thereby adopt the new origin even with old DNS down.

Stage 3 — Platform deploy (protected workflow, already covered by the task-scoped operational approval). The platform-deploy dispatch carries the candidate SHA against the replacement ECS pair behind the new Basic ALB, preserving the two-instance rolling replacement, rollback record, and attachment-storage cutover the workflow owns. Verify readiness, account login, and Relay WSS on the new www origin while the old ECS and Standard ALB remain available for rollback. Old-client behavior is verified in Stage 5, not assumed here; the disabled old origin DNS is not implicitly restored.

Stage 4 — Client reissue (formal Desktop and Mobile releases from one reviewed candidate). Bump Desktop and Mobile versions through a new Product Release Plan on `master`; the new origin is baked into the Desktop operated config and both Mobile builds at that candidate. Desktop publishes through desktop-release (signed, notarized, `--latest`). The user's formal Mobile request is product distribution to Mobile users, which the workflow splits into two separately evidenced channels: the signed Android APK as durable product distribution, and the iOS TestFlight upload as a distinct gated step reported shipped only when `upload_testflight` was requested and a validated build number exists. Neither channel alone satisfies the formal request: a GitHub prerelease by itself is not formal Mobile publication, and the TestFlight upload is a separate product-distribution gate rather than a downgrade of the request. Before any Mobile publication, verify new-domain TLS/readiness from the physical Android path (WebView, not only desktop browsers), because #480's failure lived exactly there.

Stage 5 — Physical acceptance and old-client transition evidence. Phone-side: valid TLS, fresh GitHub login preparation, same-account authentication, WSS attachment, explicit-link pairing, Remote Online, and phone-originated ping/pong — preserving user devices and Desktop instances, with sanitized evidence published through `gif-assets`. Desktop-side: update from an installed old-origin build to the new release through the GitHub updater — which stays reachable because it is independent of the Platform origin — and confirm the reissued channel. Old-client transition is proven here on the verified mechanism, before retirement of the old frontend: an installed old-origin client POSTs login and polls on the old hostname (when old DNS is separately enabled) and completes the OAuth flow against the new-domain callback the server generates, or the finding is recorded as a transition gap that blocks Stage 4's claim of a non-breaking reissue. Pending pre-cutover login attempts that fail after the switch are handled by a client restart that re-initiates the flow, and that restart requirement is recorded as transition evidence. The gestaltrun.com certificate, Standard ALB, old ECS, and preserved namespace remain available until this evidence and the rollback window close. After dependency ownership is confirmed, the authorized executor may release superseded resources; shared resources and unrelated EIPs remain. Rollback at every stage: Stage 1 restores old A values and routes traffic to the Standard frontend; Stage 2 restores the two Environment variables and the OAuth App callback; Stage 3 uses the workflow's own rollback record; Stage 4 does not unpublish old releases—old installers remain valid download targets, and rollback is re-pointing origin variables plus reissuing from the prior candidate.

## Sequencing against the in-flight release train

PR #584 carries Product Release Plan 0012 selecting Desktop 0.1.16 from `automation/product-release`, verified read-only at head `3ac00a04805a3415b68a8d4e69d5c45af816c4f3` (OPEN, Draft). That PR is not modified by this specification, and this branch's planning is coordinated against its state without depending on its head moving. The ordering rule is ledger collision, not version dependency: this cutover's Product Release Plan (Stage 4) is created on `master` only after plan 0012 has merged or been explicitly dispositioned, because two open plans over `product-releases/` would contend for `nextSequence` and the release-intent ledger. Beyond that rule the version sequence stays flexible — nothing here requires the Desktop reissue to build on 0.1.16 specifically, and this task does not dispatch #584's release on the basis of an unrelated recommendation: any release dispatch is coordinated against the freshest release-train state by whoever owns that decision, to avoid unnecessary paid or release mutations. Platform-deploy dispatches (Stage 3) do not contend with a Desktop-only plan; if 0012 has already promoted Desktop, Stage 3 may run before or after Stage 4's plan merges, but the mobile acceptance run (Stage 4) must bind to the exact new-domain candidate SHA.

## Alternatives considered

**Keep serving gestaltrun.com until its filing completes.** Rejected: the filing timeline is unbounded, the user has explicitly approved the move, and production service carries filing risk meanwhile.

**Serve the new domain without moving `PLATFORM_ORIGIN` (proxy or redirect only).** Rejected: OAuth callbacks and Relay WSS must terminate on the origin the clients and listen process actually validate; redirecting OAuth POSTs or WSS indiscriminately breaks both, and the issue text forbids it.

**Modify the Standard ALB in place.** Rejected: Standard cannot downgrade to Basic in place, and mutating the only accepted frontend would weaken rollback. The approved path creates a new Basic instance, verifies it in parallel, cuts traffic over, and releases the Standard instance only after acceptance and the rollback window.

**Use one ECS node or remove the load-balancer tier.** Rejected: the user selected two monthly `ecs.e-c1m2.large` nodes behind ALB Basic after reviewing the cost and availability tradeoff. RDS, Redis, data, database identity, namespace, and signing keys remain unconditional preservation requirements.

**Point `PLATFORM_ORIGIN` at the apex.** Rejected: the www hostname is the issued canonical authority; apex support exists so bare-domain HTTPS does not fail, not as a second authority. Two origins would double the CORS and callback surface for no benefit.

**Rename brand surfaces and historical documentation URLs to the new domain.** Rejected: the change is an origin migration, not a rebrand or history rewrite. The current served homepage canonical and discovery metadata move to the canonical www service origin; brand names and historical URLs stay unchanged.

**Reissue clients before the Platform serves the new origin.** Rejected: baked client authority must point at an origin that already passes `/readyz` and login, or every updated install breaks at first run.

## Acceptance criteria

- The selected replacement topology is staged under the recorded authority: two monthly `ecs.e-c1m2.large` instances with 40 GiB ESSD PL0 disks at a combined first-month checkout no greater than 135.04, no annual commitment, and no default auto-renew; a new ALB Basic with current terms checked against the approximately 67.56-per-720-hour planning basis; and the verified `tls_cipher_policy_1_2_strict_with_1_3`, HTTPS, WebSocket, HTTP/2, XFF, and two-backend health-check behavior. A current timestamped account and price recheck occurs immediately before mutation.
- One valid HTTPS certificate covering both apex and `www.beikejiedeliulangmao.top` terminates on the new Basic frontend; the existing Platform identity, RDS, Redis, database identity, signing keys, namespace, accounts, and durable data are preserved without a new identity system or re-keying.
- Apex and www resolve publicly through the Aliyun DNS mechanism chosen in Stage 1; the three TXT records and all unrelated DNS records are verbatim; old A-record values are recorded for rollback; certificate hostname validation passes for both names.
- `PLATFORM_ORIGIN` and `PLATFORM_GITHUB_CALLBACK` in Environment `production`, the operated Desktop config, and the Mobile build variables all name `https://www.beikejiedeliulangmao.top` with the fixed callback path; WSS and `/pair` links derive from that origin; the OAuth App `Ov23lip9LTmnFuFpFeeV` callback matches it.
- New formal Desktop and Mobile versions publish from one reviewed candidate: Desktop with signed/notarized installers and a `--latest` GitHub Release; Mobile with a candidate-bound acceptance run and both product-distribution channels evidenced separately — the signed APK as durable distribution, and TestFlight reported shipped only with a requested upload and a validated build number. The formal Mobile request is satisfied only by the channel evidence the user actually needs, never by an APK prerelease alone. Release manifests, signing artifacts, update channels, and baked-in authority are verified separately per unit.
- Physical Android acceptance passes on the new domain before formal Mobile publication: TLS, GitHub login preparation, same-account authentication, WSS, explicit-link pairing, Remote Online, phone-originated ping/pong; user devices and Desktop instances are preserved; sanitized evidence publishes through `gif-assets`.
- Old-client transition and rollback are demonstrated, not assumed: an installed old-origin Desktop updates through the origin-independent GitHub updater; an installed old-origin client POSTs login and polls on the old hostname only when that DNS is separately enabled and completes OAuth against the server-generated new-domain callback; pending pre-cutover attempts that require a restart are recorded; and each stage's rollback path carries exact pre-change values. The gestaltrun.com certificate, Standard ALB, old ECS, and namespace remain available until this evidence and the rollback window close; only then may the executor release confirmed superseded resources.
- The currently DISABLED old DNS records are not re-enabled implicitly by this change; any change to that state is a separate explicit user decision.
- The served homepage canonical and current discovery metadata use `https://www.beikejiedeliulangmao.top/`; the brand name and historical documentation URLs are unchanged.
- #480 and #415 are not closed by this change; #480's ALB/TLS diagnosis is revalidated against the new certificate and its own evidence, and #415 closes only on its own release evidence.

## Risks

- DNS TTL 600 s bounds, but does not eliminate, propagation overlap: both names may resolve during a window when the OAuth App callback accepts only one origin. Stage ordering (certificate and DNS before the Environment switch) keeps every interval serveable.
- Old-client login continuity depends on both hostnames reaching the same backend with the same identity, which the verified mechanism requires; a frontend or identity change selected in Stage 0.5 that breaks that pairing breaks old-client login completion even with the old hostname serving. Login attempts pending at cutover reference the pre-switch callback and may fail to complete until the client restarts and re-initiates; the restart is a recorded transition step, not a silent loss.
- The DISABLED state of the old gestaltrun.com DNS records is an unresolved deliberate decision owned outside this note: while it persists, old clients have no direct Platform path, and only the origin-independent Desktop updater can carry an old install to the reissued release. Re-enabling is not implied by any stage here and needs its own explicit decision.
- The smaller 2 vCPU/4 GiB replacement shape may be insufficient for observed load. Staging both nodes while the old pair remains available bounds rollback time; utilization and service acceptance must pass before the old ECS resources can be released.
- The contradictory billing reports are resolved in direction by the timestamped corrected report (balance +86.52, no ALB financial locks, 2026-09-05T16:04Z), which supersedes the earlier FinancialLocked/−13.48 reading; the durable rule is that account state is time-sensitive input, so a current timestamped recheck is taken immediately before any mutation rather than any single report being treated as settled fact.
- The 2 vCPU/4 GiB `e-c1m2.large` shape is smaller than the current instances and its sufficiency is a validated assumption, not a measurement, until Stage 0.5 checks it against observed utilization; the quoted 135.04 pair total is the purchase amount only while checkout stays within the cap. A mis-sized shape surfaces as degraded service after replacement, which the rolling replacement bounds but does not eliminate.
- The approved ALB Basic path requires a new-instance cutover. Its approximately 67.56-per-720-hour planning basis excludes public traffic, uses estimated LCU consumption, and assumes unconfirmed discounts; the executor checks current terms and stops if a safety constraint cannot be met. Parallel Basic/Standard and replacement/old ECS operation creates disclosed temporary overlap, but old-resource release before acceptance would remove rollback.
- The physical Android WebView path (#480) may fail on the new certificate for reasons distinct from the old diagnosis; Stage 4 gates publication on that path, so the risk is a blocked reissue, not a broken published build.
- Moving the homepage canonical and discovery metadata to the new www origin transfers current discovery authority before the new domain has accumulated standing; retaining the historical URLs elsewhere avoids rewriting old references but does not remove that SEO transition risk.
- Preserved identity means preserved blast radius: a bad Stage 3 deploy touches the same durable state the old domain served. The two-instance rolling replacement and the workflow's rollback record own this across the instance replacement; no new mechanism is added here.
- The new Basic listener's certificate attachment and SNI behavior must be observed before DNS cutover; the plan does not infer production behavior from feature availability alone.
